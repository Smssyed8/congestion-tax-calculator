spring:
  config:
    import:
      - "classpath:congestion-config.yml"
      - "classpath:gothenburg-config.yml"
  management:
    metrics:
      export:
        simple:
          enabled: false
      enabled: false